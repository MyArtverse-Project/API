services:

  postgres:
    image: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: devpass
    ports:
      - 5432:5432

  adminer:
    image: adminer
    restart: always
    ports:
      - 8000:8080
